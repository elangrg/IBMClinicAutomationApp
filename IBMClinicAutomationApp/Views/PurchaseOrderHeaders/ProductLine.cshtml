@model IEnumerable<IBMClinicAutomationApp.Models.PurchaseOrderDrugLine>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Qty)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.drug.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PurchaseOrderHeader.Note)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Qty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.drug.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PurchaseOrderHeader.Note)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PODrugProdLineID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PODrugProdLineID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PODrugProdLineID })
        </td>
    </tr>
}

</table>
