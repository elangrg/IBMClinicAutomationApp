@model IEnumerable<IBMClinicAutomationApp.Models.PurchaseOrderHeader>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PODate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Note)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.supplier.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PODate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Note)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.supplier.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.POID }) |
            @Html.ActionLink("Details", "Details", new { id=item.POID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.POID })
        </td>
    </tr>
}

</table>
